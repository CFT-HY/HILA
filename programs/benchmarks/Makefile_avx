ADDITION = -DMSIZE=2
OPTS = -DNDIM=4  $(ADDITION)

TRANSFORMER = ../../build/transformer -DPUHTI -DAVX -target:VECTORIZE=64 -I../../../llvm/lib/clang/8.0.1/include/ 
#TRANSFORMER = ../../build/transformer -DAVX -target:AVX
CC = mpic++ -g -mavx512f -mavx2 -mfma -DAVX

CFLAGS = -Ofast $(OPTS)
CXXFLAGS = -Ofast -x c++ --std=c++17 $(OPTS)

%.exe: %.tr.o Makefile inputs.o mersenne_inline.o lattice.o memory.o setup_layout_generic.o map_node_layout_trivial.o 
	$(CC) -o $@ $< inputs.o mersenne_inline.o lattice.o memory.o setup_layout_generic.o map_node_layout_trivial.o

%.cpt: %.cpp Makefile
	$(TRANSFORMER) $<

%.tr.o : %.cpt
	$(CC) $(CXXFLAGS) $< -c -o $@

%.tr.s : %.cpt
	$(CC) $(CXXFLAGS) $< -S -o $@


%.o : ../plumbing/%.cpp
	$(CC) $(CXXFLAGS) $< -c

%.o : %.c
	$(CC) $(CFLAGS) $< -c

clean:
	rm -rf *.o *.cpt

cleanall:
	rm -rf *.o *.cpt *.exe
